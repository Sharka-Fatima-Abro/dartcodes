void main() {
  // Creating a simple map with key-value pairs
  Map<String, int> ages = {
    'fatima': 30,
    'sharka': 25,
    'sundas': 35,
  };

  // Accessing values in the map
  print(ages);

  print(ages["fatima"]);
}
